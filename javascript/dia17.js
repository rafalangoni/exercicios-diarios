// Write a function that takes a string (a) as argument
// Extract the first half a
// Return the result

function extraiMetadeString(texto) {
  return texto.slice(0, texto.length / 2);
}

console.log(extraiMetadeString("Raspfiuqwi1bwiuecfael"));

function myFunction(a, n) {
  return a[n];
}

console.log(myFunction('Rafael',2))
