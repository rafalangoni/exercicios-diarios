// Write a function that takes a string (a) and a number (n) as argument
// Return the nth character of 'a'

const posicaoLetra = (palavra, posicao) => {
    return palavra.charAt(posicao)
}

console.log(posicaoLetra('Rafael',2))