// Write a function that takes a string (a) as argument
// Get the first 3 characters of a
// Return the result

const nome = 'Rafael'

const retorna3PrimeirasPosicoes = (texto) =>{
    return texto.slice(0,3)
}

console.log(retorna3PrimeirasPosicoes(nome))

